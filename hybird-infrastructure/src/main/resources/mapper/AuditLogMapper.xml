<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wyman.infrastructure.dao.mapper.AuditLogMapper">

    <resultMap id="BaseResultMap" type="org.wyman.infrastructure.dao.po.AuditLogPO">
        <id column="log_id" property="logId" jdbcType="BIGINT"/>
        <result column="event_type" property="eventType" jdbcType="VARCHAR"/>
        <result column="event_data" property="eventData" jdbcType="LONGVARCHAR"/>
        <result column="operator" property="operator" jdbcType="VARCHAR"/>
        <result column="ip_address" property="ipAddress" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        log_id, event_type, event_data, operator, ip_address, create_time
    </sql>

    <insert id="insert" parameterType="org.wyman.infrastructure.dao.po.AuditLogPO">
        INSERT INTO audit_log (
            event_type, event_data, operator, ip_address, create_time
        ) VALUES (
            #{eventType,jdbcType=VARCHAR}, #{eventData,jdbcType=LONGVARCHAR},
            #{operator,jdbcType=VARCHAR}, #{ipAddress,jdbcType=VARCHAR},
            #{createTime,jdbcType=TIMESTAMP}
        )
    </insert>

    <select id="selectByEventType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM audit_log
        WHERE event_type = #{eventType,jdbcType=VARCHAR}
        ORDER BY create_time DESC
        LIMIT 100
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM audit_log
        ORDER BY create_time DESC
        LIMIT 100
    </select>

</mapper>
